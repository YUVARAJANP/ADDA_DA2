pipeline {
  agent any
  environment {
    PATH = "D:/DevTools/apache-maven-3.9.4/bin$PATH"
  }
  stages {
    stage('Git Repo Checkout') {
      steps {
        checkout scm
      }
    }
    stage('Repository Clone') {
      steps {
        checkout scm
      }
    }
    stage('Build') {
      steps {
        checkout scm
      }
    }
    stage('Docer Build') {
      steps {
        checkout scm
      }
    }
    stage('Docer Push') {
      steps {
        checkout scm
      }
    }
  }
}
